# 智能体操作手册

> Mon Mode de Vie（MMV）当前维护基准。执行任务前先读项目根目录的 `AGENTS.md` 与 `CONTEXT.md`，再读本手册中对应章节。
>
> 本手册以实际代码和用户最近一次明确要求为准；不要复活历史独立页、旧构建流程或已经废弃的内容加工规则。

---

## 一、不可违背的产品约定

1. **主应用是唯一正式界面。** 正式入口为 `index.html`，日记列表为 `#journal`，单篇日记为 `#journal/YYYY-MM-DD`。`journals/YYYY-MM-DD.html` 只是主应用读取的文章载荷和旧地址跳转入口，不是另一套可见页面。
2. **日记原文优先。** 用户提供的 `YYYY-MM-DD.txt` 是该日记正文的唯一内容来源。除第 3.2 节明确授权的“重复昵称行折叠”外，必须尊重原字、原句、原段落和原有语气，不擅自润色、摘要化、扩写或翻译成另一篇文章。
3. **不再创建“日记随笔源文本”。** 有 TXT 时直接把它呈现为正文；不得在同一页再复制一份，也不得增加“日记随笔源文本”标题、附件卡片或来源气泡。
4. **只有确实没有正文时才用占位句。** 中文固定为：`今天的日记没有正文文本啦`。此时为了四语界面不出现空白，可使用：
   - English: `There is no written text for today's journal entry.`
   - Français: `L’entrée du jour ne contient pas de texte.`
   - Deutsch: `Der heutige Tagebucheintrag enthält keinen Text.`
5. **媒体与正文是并列内容。** 图片进入宫格；视频始终是独立播放器，不与图片或其他视频塞进宫格，不裁成统一缩略画幅。
6. **地址必须可收藏、可前进后退。** 打开模块或具体内容时，地址栏同步变化；单篇日记的标准深链接只能是 `#journal/YYYY-MM-DD`。
7. **本地与线上都要真实点击。** HTTP 200、文件存在、代码无语法错误都不能代替浏览器交互测试。

---

## 二、当前项目结构

```text
D:\Mon Mode de Vie\
├── index.html                         主应用外壳与模块容器
├── route.js                           Hash 路由工具
├── script.js                          全站状态、四语 UI、主题、日历、OTD 等
├── styles.css                         全局样式与 motion tokens
├── journal-data.js                    日记清单加载与标准化
├── journal-module.js                  日记列表、内嵌阅读器、深链接、媒体编排
├── journal-module.css                 日记列表、阅读器、图片宫格、媒体样式
├── journals\
│   ├── YYYY-MM-DD.html                文章载荷；直接访问时跳回主应用深链接
│   ├── index.html                     兼容入口；不得发展成第二套 UI
│   ├── data\
│   │   ├── journals.json              日记清单
│   │   ├── journals.js                同一清单的内嵌副本
│   │   └── source-texts\              TXT 原始存档，可保留但不重复展示
│   └── assets\
│       ├── images\                    图片、海报和网页视频
│       ├── video\                     HLS/长视频资源（按需）
│       ├── journal-player.js           Plyr + HLS 增强
│       ├── vendor\                    本地播放器依赖
│       └── lightbox.*                 图片灯箱
├── tools\
│   ├── start-local.ps1                自动寻找可用端口并启动本地预览
│   ├── collapse_journal_shells.py     把旧单页收敛为载荷与重定向
│   └── import_download_journals.py    批量导入工具；使用前必须先审查规则
├── 启动网站.cmd                       推荐的本地启动入口
└── .github\workflows\deploy.yml       GitHub Pages 部署
```

`tools/build_journals.py`、旧四语生成器和历史校验脚本可能记录旧架构，**不能因为文件仍存在就视作当前事实**。调用前先检查它会不会重建独立外壳、覆盖原文或把媒体复制进每个语言区。

---

## 三、从下载目录导入一批日记

### 3.1 先盘点，不要猜

1. 列出下载目录中新出现的 TXT、图片和视频，按日期分组。
2. TXT 文件名优先确定日记日期；媒体可按文件名日期归属，但遇到孤立文件、日期冲突或文本明确提到却缺失的媒体时，只报告缺口，不虚构归属。
3. 读取 TXT 时使用 UTF-8，并保留换行、标点、特殊字符和口语表达；网页正文中唯一允许省略的内容见第 3.2 节“重复昵称行折叠”。
4. 检查同日期日记是否已存在；更新时必须幂等，不能重复插入正文、媒体或清单条目。
5. 导入前运行 `git status --short`，识别并保护用户已有修改和未跟踪文件。

### 3.2 正文规则

- 有 TXT：TXT 是正文的唯一内容来源。HTML 转义只用于安全呈现；除下述唯一例外外，网页展示不得改变文字内容。
- **时间标记是原文、正文的一部分。** 形如 `编辑于2024年10月11日 12:24`、`2024年10月7日 21:05` 的时间戳必须逐字保留，不得当作元数据忽略、删除或折叠。
- **带括号的媒体提示是「照做」指令，不是原文。** 形如 `（插图1到4）`、`（注意，这里放本日最后一张插图）`、`(注意力，这里就放2024年10月7日的第一张插图)` 应替换成对应媒体，并从展示副本中删除该提示文字本身；它不进入正文，也不应被翻译成任何语言的文字。
- **唯一获授权的轻量规范化例外——重复昵称行折叠：** 若某一物理行去除首尾空白后只包含昵称，且它的下一物理行紧接着以完全相同的昵称开头，并构成 `昵称：评论` 或 `昵称 回复某人：评论`（允许原文使用半角/全角冒号及原有空格），网页正文展示时删除前一行，只保留后一条完整评论行。
- 上述规则不得跨越空行、日期、时间、媒体提示或其他正文，不得用于不同昵称，也不得删除没有完整评论内容的行。识别时可以忽略行首尾空白，但保留下来的评论行必须按原文呈现；不得借机合并评论、改标点、改昵称或做一般性去重。
- 这是**展示层唯一例外**。下载目录中的原始 TXT 与 `journals/data/source-texts/YYYY-MM-DD.txt` 校验存档必须保持原样，不能回写删除昵称行。批量导入后应逐篇扫描这一严格相邻模式，并同时确认没有误删未命中的行。
- 不得先写“提炼版正文”，再把 TXT 放到“源文本”章节。
- 除上述重复昵称行折叠外，不得自动校对错别字、统一标点、重排句序、去重或翻译。若怀疑原文有误，先保留原文并向用户说明。
- 当前这类原始 TXT 日记不要求凭空生成英、法、德正文。切到其他语言时仍可保留作者原文；语言切换只负责应用 UI。若用户另行提供译文或明确要求翻译，才增加译文。
- 无 TXT、无任何可识别正文：才使用第二节规定的四语占位句。
- `journals/data/source-texts/YYYY-MM-DD.txt` 可以作为校验与恢复副本，但必须与收到的源 TXT 保持一致；网页不得再以附件、来源模块或第二段正文显示它。

### 3.3 文章载荷

每篇日期 HTML 至少应包含：

- 安全、隐藏的载荷外壳；直接访问时立即跳转到 `../index.html#journal/YYYY-MM-DD`。
- 指向同一标准深链接的 canonical。
- 一个 `.journal-article`，包含日期、地点（若确有）、正文以及共享媒体。
- 正文段落按 TXT 原换行呈现；仅可在命中第 3.2 节严格条件时，从展示副本中省略紧邻完整评论前的重复昵称行。
- 媒体只写一份共享结构，不复制到中文、英文、法文、德文区。
- 当 TXT 含媒体占位提示，或用户明确指定媒体应跟随的正文段落时，使用导入脚本中的 `MEDIA_PLACEMENTS` 按叙事锚点插入媒体。`replace` 模式只从网页展示副本中省略占位提示，下载目录 TXT 与 `source-texts` 存档仍保持原样。任何配置锚点缺失、媒体数量不足或存在未消费媒体都必须让导入失败，禁止静默把媒体移到文末。
- **无占位提示时，媒体统一放正文末尾图集。** 若 TXT 没有任何括号媒体提示，本日图片/视频按文件名自然排序后集中在正文末尾以图集呈现；这不算「静默移到文末」，因为原文没有给出任何叙事锚点。
- **媒体序号按自然排序分配，避免扩展名冲突。** 无后缀的 `YYYY-MM-DD.jpg` 与 `YYYY-MM-DD.png` 是两张不同的图，必须分配连续序号（jpg 在前、png 在后），不能都映射为第 1 张导致 `dict[序号]` 相互覆盖丢图；`_N` 后缀按数字顺序接续编号。
- **同日期多个 TXT 文件要按完整度去重。** 桌面可能出现 `2025-01-28.txt` 与 `2025年1月28日.txt` 两个文件、其中一个是残缺版；按字节数最大保留完整版，避免残缺版覆盖完整正文。
- **散文体+时光轴整合必须幂等。** 把时光轴记录追加到已有散文体日记前，先移除旧的「时光轴记录」块，再插入新块；重跑不得重复追加。
- **媒体文件名要兼容三种序号与中文日期。** 图片可能是 `YYYY-MM-DD.jpg`（无后缀）、`_N` 后缀、` (N)` 括号序号；视频可能是 `YYYY-MM-DD.mp4`、`YYYY-M-D`（单数字月日）、`YYYY年M月D日` 中文日期 + `(N)`。盘点时都要算上；日期以文件名开头的日期标记为准，括号内序号不算日期。
- **有媒体、无日记的日期要补占位日记。** 若某日期只有图片/视频、没有 TXT 正文，创建占位日记：正文用第 4 条固定占位句（含四语占位句），媒体按自然排序放图集，excerpt 取占位句。
- **补媒体前先做哈希对账。** 用 8×8 灰度感知哈希对比桌面图与网站已有图（差异 ≤4 字节视为同一张），只补真正缺失的，避免重复导入；网站已有文件可能是 `日期.ext`（无 `-N`）格式，必须一并纳入对比。

旧地址仍可保留用来兼容书签，但**旧视觉外壳不得出现**。不要给旧地址建立另一套导航、主题、播放器或文章版式。

### 3.4 清单数据

`journals/data/journals.json` 与 `journals/data/journals.js` 必须保持完全同源，并按日期从新到旧排列。常用字段：

同一日记的媒体清单顺序必须与正文中的首次出现顺序一致；该顺序同时决定日记首页使用哪一张图片或哪一个视频 poster 作为首预览，不能继续沿用与正文叙事不一致的旧文件编号顺序。

**`location` 字段必须填写。** 日记模块初始页（列表）每条都要显示地点。若原文明确提到地点，按原文填写；若原文未提及地点，**缺省值固定为 `湖北省武汉市洪山区`**，不得留空。

**散文体原创日记必须有 `locales` 四语元数据。** 字段结构为 `locales: { en: { location, excerpt }, fr: { location, excerpt }, de: { location, excerpt } }`，供 OTD 卡片和日记列表在切换英/法/德文时显示对应语言的摘要和地点，避免四语界面空白。聊天记录型日记（location 为"微信聊天记录"）不需要 locales。

```js
{
  date: "YYYY-MM-DD",
  file: "YYYY-MM-DD.html",
  location: "地点（缺省：湖北省武汉市洪山区）",
  excerpt: "直接从原文截取的简短预览，不改写意思",
  images: [
    { file: "assets/images/YYYY-MM-DD-1.webp" },
    {
      file: "assets/images/YYYY-MM-DD-video.mp4",
      poster: "assets/images/YYYY-MM-DD-poster.webp"
    }
  ],
  source: { label: "原创" },
  locales: {
    en: { location: "...", excerpt: "..." },
    fr: { location: "...", excerpt: "..." },
    de: { location: "...", excerpt: "..." }
  }
}
```

注意：`images` 是历史字段名，当前实际是**图片与视频的混合媒体清单**。不要只因名字叫 `images` 就把视频计作图片。列表展示时必须按扩展名分别统计：例如 `图片3张 · 视频1个`、`视频1个`、`纯文字`。若只有视频，绝不能显示“图片1张”。

原始 TXT 日记不要再写 `source: { label: "日记随笔源文本" }`。清除旧标签时同时检查四语 `sourceLabel`，避免列表残留第二行错误来源说明。

### 3.5 图片排版

- 图片预览使用横向 `4:3` 容器和 `object-fit: cover`，适用于单图、四宫格、九宫格。
- 1 张图片可单独宽幅呈现；2–4 张使用二列宫格；5–9 张使用三列宫格。
- 超过 9 张时仅展示前 9 格，第 9 格覆盖 `+剩余数量`；点击后灯箱必须能浏览全部图片，并提供上一个/下一个控制。
- 宫格裁切只影响预览，不改变灯箱中的原图比例。
- 图片转换优先 WebP，体积尽量小。
- **必须为每张日记图片生成 `_thumb` 缩略图。** "去年今日"（OTD）卡片的代码会把首图文件名替换为 `_thumb.webp`（如 `2026-08-28-illustration-01.webp` → `2026-08-28-illustration-01_thumb.webp`）作为预览图。若不生成 `_thumb`，OTD 卡片将无图可显示。缩略图惯例：宽度 400px，保持原始比例，WebP 格式。

### 3.6 视频排版与转码

- **每段视频都是独立播放器。** 即使同一篇有多段视频，也逐个纵向排列；视频与图片相邻时，先组成纯图片宫格，再在合适的叙事位置放独立播放器。
- 播放器使用原始视频宽高比，不套 `4:3` 图片格，不用 `object-fit: cover` 裁内容。海报与播放画面必须保持同一比例。
- 横屏视频：在正文宽度内尽量宽，受阅读器最大宽度限制。
- 竖屏视频：居中显示，按视频实际比例收窄最大宽度；可用克制的背景/留白承接两侧空间，但不得把画面拉伸或裁成横屏。
- 方形或非常规比例：尊重元数据中的宽高比，容器自适应。
- 播放器保留原生可访问语义，并由 `journal-player.js` 增强为 Plyr 风格；需要 HLS 时使用本地 hls.js，自适应失败后回退 MP4。
- 日记播放器保留全屏，但暂不提供画中画（PIP）。部分 Chromium / Edge 环境在进入 PIP 时会触发渲染进程级 `STATUS_ACCESS_VIOLATION`，这类崩溃无法由网页捕获；播放器控件不得重新加入 `pip`，视频元素也应保留 `disablepictureinpicture`，直至完成跨浏览器回归并确认上游问题已消失。
- 普通 MP4 应转为浏览器普遍支持的 H.264 视频 + AAC 音频，并启用 fast start；长视频可增加 HLS 多码率版本。不要把 HEVC-only 文件直接当通用网页视频。
- 对视频、HLS 分片做 Range/分段响应验证。单文件必须满足 GitHub 的文件大小限制；不能只在本机能播。
- 列表预览可使用同宽高比海报与播放标识，但不要自动播放，不要把海报伪装成“图片”。

### 3.7 与其他模块的联动

- “去年今日”（OTD）当前由 `MMVJournalData` 动态读取，不再手工维护固定 HTML 日期、固定摘要或固定图片。
- OTD 若首个媒体是视频，应使用视频 poster 或文字预览，不能机械拼出不存在的 `_thumb.mp4`。
- OTD 若首个媒体是图片，**必须存在对应的 `_thumb.webp` 缩略图**（见 3.5 节），否则 OTD 卡片无图。
- **每日茶话（纪念日故事）是每次更新日记的必做配套，必须存档。** 每次新增或更新一篇日记时，都要为该日记日期在 `script.js` 的 `as` 对象中创建四语茶话（中/英/法/德各一份），格式为 `"YYYY-MM-DD": ["气泡1", "气泡2", ...]`，以"我翻到了今天的日记。"开头、以"以上，今天的茶话。"结尾，内容基于当天日记改编，不擅自扩写或虚构未发生的事。茶话取数逻辑用 `new Date().toISOString().slice(0,10)` 取当天 UTC 日期，fallback 取字母排序最大的日期键。`as` 对象是点击"我想听听今天的纪念日茶话"按钮时在 handler 内定义的局部四语三元对象，必须四语同步更新。
- 相册、日历、音乐盒、作者留言都不是日记导入的默认联动项。

### 3.8 四语译文（用户明确要求时）

- 时光轴/散文体日记的四语正文，格式为：中文 `data-journal-original`（始终显示）+ 三个 `data-journal-lang="en/fr/de"` 的翻译 section（`hidden`，按 UI 语言切换）。媒体只放中文区，四语 section 只含文字。
- 散文体/PDF 来源日记的四语正文在 `<script id="journalI18n">` 的 JSON 里（`locales.zh/en/fr/de.blocks`），由 JS 渲染，不要混用成 `data-journal-lang`。
- 翻译段落数必须与中文段落数一一对应，不增删、不合并、不拆分；时间标记（`编辑于…`、日期时间）按目标语言自然转换，对话体行保留昵称原文、只译冒号后内容，占位符提示不翻译成文字。
- 生成四语后用 `lxml` 精确核对每个 `section` 的段落数，不要用 `.*?</section>` 这类惰性正则统计——它会被媒体 `div` 或嵌套结构干扰而误报。

---

## 四、路由与唯一界面

### 4.1 标准链接

| 内容 | 标准链接 |
|---|---|
| 会客厅 | `#home`（以当前路由实现为准） |
| 日历 | `#now/...`（年/月/周/日详情须保留当前路由状态） |
| 日记列表 | `#journal` |
| 单篇日记 | `#journal/YYYY-MM-DD` |
| 相册 | `#album/...`（分册与具体照片须可收藏） |

日记代码只接受 `YYYY-MM-DD` 日期格式。点击日记应使用真实 `<a href="#journal/YYYY-MM-DD">`，JavaScript 再增强交互；这样脚本尚未加载时也能复制链接。

### 4.2 历史 URL

`/journals/YYYY-MM-DD.html` 可以继续存在，用来：

1. 给主应用 `fetch` 文章载荷；
2. 接住旧收藏并立即重定向到 `/#journal/YYYY-MM-DD`。

它不是“独立日记页”，不应被搜索引擎收录，也不应显示旧导航或旧主题。`/journals/index.html` 同理，只作为兼容入口。修改载荷模板后，抽查直接打开旧 URL 是否看不到旧外壳。

### 4.3 中断与快速操作

- 连续点击不同日记、立即返回、浏览器前进/后退时，以最新路由为准，过期请求不能覆盖新页面。
- 退出动画完成前再次操作时，不得把列表和详情同时隐藏。
- 失败提示“日记暂时没有摆好。”只能用于真正的载荷读取失败；没有正文应显示正文占位句，不能当请求错误处理。

---

## 五、本地预览与真实点击测试

### 5.1 启动

双击：

```cmd
启动网站.cmd
```

它调用 `tools/start-local.ps1`，会在 `4174` 到 `4194` 中自动选择空闲端口并打开浏览器。**不要假设一定是 4174**；以终端实际显示的 URL 为准。若全部被占用，先查明现有服务，不要硬杀无关进程。

静态站点必须通过 HTTP 服务测试，不能双击 `index.html` 用 `file://` 打开，因为文章 `fetch`、模块资源、HLS 和路由行为会不同。

### 5.2 日记发布前必测

用真实浏览器逐项点击，至少覆盖本批第一篇、媒体结构最复杂的一篇、单视频篇、无正文篇：

1. 打开实际本地 URL，点击“日记”。
2. 列表中每条的 `图片N张`、`视频N个`、`纯文字`是否正确；只有视频的条目不得出现“图片”。
3. 点击条目后地址栏是否变为 `#journal/YYYY-MM-DD`。
4. TXT 是否为正文唯一内容来源，且没有“日记随笔源文本”标题或重复原文；若折叠了昵称行，是否只命中第 3.2 节规定的严格相邻同名模式，保留下来的完整评论行未被改写，源 TXT 存档未改变。
5. 图片是否进入正确宫格；超过 9 张的计数和灯箱前后切换是否正确。
6. 每段视频是否为独立播放器，横屏、竖屏与海报比例是否正确；播放、暂停、拖动、音量、键盘焦点、全屏均可用。
7. 返回按钮、浏览器后退/前进、刷新深链接、快速切换两篇日记是否正常。
8. 切换四种 UI 语言，原始 TXT 不被替换成智能体自写内容，应用控件不空白。
9. 开启 `prefers-reduced-motion` 后，信息和功能仍完整。
10. 浏览器控制台无异常，请求面板无 404；MP4/HLS 能请求并持续播放。

不能把“我检查了 HTML”写成“我点击测试了”。无法启动浏览器时要如实说明未完成的验证项。

---

## 六、代码、媒体与缓存验证

### 6.1 静态检查

- 修改 JavaScript 后运行 `node --check`（至少覆盖实际改动的 JS）。
- 解析 `journals.json`，确认无重复日期、按日期倒序、每个 `file` 存在。
- 比对 `journals.json` 与 `journals.js` 的内容，确认二者同步。
- 遍历清单中的每个媒体路径和 poster，确认文件存在、大小合理。
- 检查新增 HTML 只有一份正文，没有旧来源标题，没有媒体重复。

不要盲信已经过时的校验脚本。若脚本仍强制“四语文章、四份媒体、旧独立页”，先更新脚本或改用针对当前架构的小范围检查。

### 6.2 媒体检查

- 用媒体探测工具读取 codec、宽、高、像素宽高比、时长与音轨。
- MP4 通用基线：H.264 + AAC + fast start。
- 单视频海报应从该视频生成并保持同一比例。
- HLS 要检查 master playlist、各清晰度 playlist 和分片路径。
- GitHub 单文件不能达到或超过 100 MB；建议在接近限制前就压缩或切 HLS。

### 6.3 缓存

GitHub Pages/CDN/浏览器可能保留旧 JS、CSS 或曾经的 404。修改会影响已上线资源时：

- 更新 `index.html` 中对应静态资源版本参数；
- 文章载荷读取保持 `cache: "no-store"`，必要时更新文章请求版本；
- 不要只让用户反复 `Ctrl + F5`，先确认线上 HTML 实际引用了新版本。

---

## 七、发布与线上验收

### 7.1 提交前

1. `git status --short`：只暂存本任务文件；未跟踪或无关修改默认属于用户。
2. 查看 diff，特别检查 TXT 原文是否被改写、媒体统计是否把视频算成图片、是否意外恢复旧外壳。
3. 完成第五、六节的本地验证。
4. 禁止用 `git add -A` 不加审查地把整个工作区打包提交；使用明确文件列表或审查后的范围。

建议提交说明：

- `diary: import YYYY-MM-DD journal`
- `fix: separate journal video players`
- `docs: refresh agent manual`
- `ci: update Pages deployment`

### 7.2 推送后

1. 确认 GitHub Actions 的 Pages workflow 成功；提交成功不等于部署成功。
2. 打开 [newtnorlly.xyz](https://newtnorlly.xyz/) 进行与本地相同的真实点击流程。
3. 至少实测一个完整深链接，例如 `https://newtnorlly.xyz/#journal/2024-09-11`。
4. 在线播放新视频，并观察首帧、时长、拖动、全屏和 Range/HLS 网络请求。
5. 直接访问旧地址 `/journals/YYYY-MM-DD.html`，确认它跳到主应用标准深链接且没有旧外壳。
6. 若失败，先看线上实际版本、控制台和网络请求，再判断是代码、路径、格式、缓存还是部署问题；不要连续猜原因。

---

## 八、其他模块的当前维护原则

### 8.1 日历

- 日程数据位于 `calendar-data.js`，通过 `window.MMV_CALENDAR_SCHEDULES` 暴露。
- 保持 `id` 唯一，日期时间使用 ISO 格式；标题需要四语 UI 映射时维护 `script.js` 的 `CAL_I18N`。
- 年/月/周/日视图的路由必须随选择更新，并可刷新恢复。

### 8.2 相册

- 数据位于 `album-data.js`，交互位于 `album-module.js`，样式位于 `album-module.css` / `album-gallery.css`。
- 当前有 7 个既有分册。除非用户明确要求改变信息架构，不擅自增删分册。
- 图片优先使用 WebP 与独立缩略资源；每个实际引用路径都要验证，不按文件名猜文件存在。
- 分册与具体照片应使用当前相册深链接，支持刷新、收藏和前进后退。

### 8.3 音乐盒、作者留言与主题

- 音乐资源位于 `assets/music/`；改曲目时同步播放器源和四语说明，并实际播放。
- 作者留言位于 `script.js` 的 `L.*.letterContent`；只在用户明确要求时修改。
- 主题由全站 tokens 与 `data-theme` 驱动。不要在日记载荷中另建一套主题系统。

### 8.4 动效与可访问性

- 动效服务层级、状态变化和空间连续性；优先 `transform` 与 `opacity`。
- 使用统一 motion tokens、自然且响应迅速的 easing/spring，支持中断与快速反向操作。
- hover、press、focus、进入与退出都要可预测；不能依靠动画传达唯一信息。
- 所有新增动效支持 `prefers-reduced-motion`；所有播放器、灯箱、返回和翻页控件支持键盘与清晰焦点。
- 样式放在所属 CSS 文件中并控制作用域。不要把“内联样式优先级高”误当成布局规范，也不要用 `!important` 掩盖错误的选择器或容器设计。

---

## 九、安全红线与事故教训

1. **禁止 `git clean`。** 它会删除尚未跟踪、但可能正是本次新增的日记和媒体。任何删除先解析明确绝对路径、确认范围，再逐项操作。
2. **保护用户工作。** 不使用 `git reset --hard`、`git checkout --` 等破坏性命令恢复整个工作区；不碰无关的未跟踪文件。
3. **批量脚本必须幂等且可审查。** 可以用脚本处理重复导入和转码，但先在少量样本验证，运行后逐篇对比。旧教训不是“永远禁止批量脚本”，而是禁止未经验证的宽泛替换。
4. **保持 UTF-8 原文。** 不因担心非 ASCII 就把中文、法文或德文改成拼音/ASCII；正确做法是指定 UTF-8、避免错误 shell 转码，并验证读回结果。
5. **先查现状再下结论。** 404 可能来自路径、文件名、部署、缓存或格式；“代码一定正确、媒体一定错误”不是有效诊断。
6. **修改压缩或单行 JS 后立即语法检查。** 括号、引号或编码损坏可能让全站交互一起失效。
7. **不把实现偶然写成永久规则。** 固定行号、固定端口、固定媒体文件名、每种语言复制媒体、强制内联 CSS、每篇日记强制 20 条茶话都属于已废弃做法。

---

## 十、日记发布核对表

- [ ] 下载目录已按日期盘点，孤立或缺失媒体没有被猜测归属
- [ ] TXT 是正文唯一内容来源；除严格相邻的同名重复昵称行外，没有摘要替代、润色、扩写、一般性去重或擅自翻译
- [ ] 重复昵称行折叠只发生在网页展示层，完整评论行原样保留，下载目录 TXT 与 `source-texts` 存档均未改动
- [ ] 页面没有“日记随笔源文本”标题、附件块、重复原文或残留 source label
- [ ] 真正无正文时才显示固定占位句
- [ ] `journals.json` 与 `journals.js` 同步、倒序、无重复日期
- [ ] 每条日记的 `location` 字段已填写；原文未提及地点时缺省为 `湖北省武汉市洪山区`
- [ ] 散文体原创日记已补 `locales` 四语元数据（en/fr/de 的 location + excerpt），OTD 与列表切四语不空白
- [ ] 每张日记图片已生成对应的 `_thumb.webp` 缩略图（宽 400px，保持比例），OTD 卡片有预览图
- [ ] 已为该日记日期在 `script.js` 的 `as` 对象中存档四语茶话（中/英/法/德），以"我翻到了今天的日记。"开头、"以上，今天的茶话。"结尾
- [ ] 列表分别显示图片与视频数量；单视频条目只显示"视频 1 个"
- [ ] 图片宫格符合 1 / 4 / 9 / 9+ 规则，灯箱可浏览全部图片
- [ ] 所有视频均为独立播放器，海报与画面尊重原始横竖比例
- [ ] 视频 codec、文件大小、fast start、Range/HLS 与线上路径已验证
- [ ] 单篇地址是 `#journal/YYYY-MM-DD`，旧 URL 会跳回该链接且不出现旧外壳
- [ ] 本地用实际分配端口完成点击、播放、返回、刷新和连续操作测试
- [ ] 四语 UI、键盘焦点与 reduced motion 正常
- [ ] JavaScript 语法、控制台、网络 404 和媒体路径检查通过
- [ ] 暂存范围经过审查，没有带入用户无关文件
- [ ] GitHub Pages 部署成功，并在线重复真实点击与播放测试

---

## 十一、本次（2026-09-16）变更记录：Outlook 日历分类配色、游戏暗色同步、苹果风二次走查

### 11.1 Outlook 日程配色忠实复刻 categories（任务核心）

- **旧缺陷**：`tools/build_public_outlook_calendar.py` 早期只按「日历名」分 3 类，结果默认日历里上千事件被统一染成 `mmv-outlook` 蓝，Outlook 里手工打的「配色/花纹标记」（categories / masterCategories）全部丢失。
- **新口径**（`ui_calendar_for_event`，优先级从上到下）：
  1. 节假日 / 生日日历先按 `calendar_kind` 归 `mmv-holiday` / `mmv-birthday`；
  2. 事件自带 categories 命中 `CATEGORY_TO_UI` 映射 → 用该色（`source="category"`，最忠实）；
  3. 早期没打分类的旧课表，按标题子串命中 `COURSE_KEYWORDS`（宏观经济/刑法/民法/乒乓球/侃哥读外刊…）兜底为 `mmv-study`（`source="course-fallback"`）；
  4. 都不中才落 `mmv-outlook` 中性蓝。
- **映射表**：睡眠→mmv-sleep 紫、三餐/橙色类别→mmv-eat 橙、课程→mmv-study 靛、午睡→mmv-nap 品红、放风/出门踏青/绿色类别→mmv-walk 绿、灵感畅想/特别安排/黄色类别→mmv-make 紫、不容忽视/红色类别/Red Category→mmv-play 粉、蓝色类别→mmv-outlook 蓝。
- `clean_categories` 去空去重保序，原样保留到 `outlookCategories` 字段，供前端图例与审计追溯（配色靠 calendarId，原始分类名不丢）。
- **强校验不可省**：构建末尾 `represented == expected`（单事件 + 循环系列展开数必须等于源事件实体数），不一致直接 `raise`；并输出 `sourceKindCounts`（category / course-fallback / holiday / outlook 各多少），本次 1431 个事件实体：eat366 / holiday285 / study398（category+course-fallback）/ sleep123 / nap122 / walk122 / make5 / play2 / outlook8。**以后重拉日历必须先看到 represented 守恒、sourceKind 分布合理，才算成功。**
- 色相只在 `calendar.css` 的 `[data-calendar-id] --ch/--cs` 变量里柔和化，明/暗两套（暗色不是反色，单独调表面与对比）；新增类别只改 Python 映射表，不要在前端硬编码颜色。

### 11.2 信息密度按视图区分（月视图降噪、日视图保详情）

- 月视图全天胶囊空间极小：隐藏教师/地点等竖排长文本，只留标题，避免一格被一条日程撑高、整月网格参差。
- 日视图时间块空间充裕：保留时间、地点、教师等竖排详情。
- 原则：**同一数据，在越概览的视图越要收敛，在越聚焦的视图越要完整**，不要为了「一致」在月视图也堆满，也不要在日视图过度删减。

### 11.3 游戏 iframe 明暗/语言同步（跨文档主题最容易漏网）

- 现象：父页面切暗色后，内嵌 `game/` iframe 仍是亮色，形成「白底漏网」。根因是 iframe 是独立 document，父页 CSS 变量与 `data-mode` 不会自动透传。
- 解法（三处缺一不可）：
  1. 父页 `script.js` 新增 `mmvGameSync()`：在主题/语言切换、初始化时向 iframe `postMessage({language, mode, active, reducedMotion})`；
  2. 子页 `game/game.js` 提 `LIGHT_COLORS` / `DARK_COLORS` 两套完整色板（键必须一一对应，本次各 9 键），`applyTheme` 整板替换并重绘 canvas（canvas 不是 DOM，不会随 CSS 变量自动变色，必须手动重绘）；
  3. `game/game.css` 补 `:root[data-mode=dark]` 下同一组变量。
- **防缓存铁律**：iframe 内部 css/js 改动后，既要 bump 内部 `game/index.html` 的引用版本串，也要 bump 父页 `index.html` 里 iframe `src` 的 `?v=` 查询串并硬刷新，否则浏览器继续用旧 iframe 缓存，看起来「改了没生效」。

### 11.4 苹果风二次走查结论：同源项目先 diff，克制不滥改

- 本站与 lectures-et-echos、Cahier 同源（共享 global 设计系统）。二次走查先用 difflib 对比三者 global.css / modules.css，**区分「功能分叉」与「真实缺陷」**：
  - lectures 多出的是 7 套明亮主题 + 小云主题切换器；Cahier 有自己的明暗双模与 View Transitions 圆形破口切换动画——这些是**有意的产品分叉，不是谁落后**，绝不能拿一个去覆盖另一个，否则会抹掉明暗双模/转场动画。
  - lectures 书架书脊 `.shelf-spine` 的 `--cloth` 低饱和莫兰迪布面色（墨绿/砖红/赭石/灰蓝…）是「布面教材」拟物隐喻，**刻意不鲜艳，不要「提亮优化」**。
  - 缺封面卡片的冷灰底只在封面缺失时出现，补封面后即被覆盖，不是缺陷。
- 结论：苹果设计七原则里 **Restraint（克制）优先**——连续圆角/发丝边/分层柔影/reduced-motion/玻璃浮层都已齐备时，不做破坏性大改；本次唯一实质修复落在本站（11.1–11.3）。新增任何渐变/玻璃/阴影/动效前先回答「是否帮助理解/操作/层级/反馈」，答不上就删。

### 11.5 环境与验证教训

- Windows PowerShell 内联 Python 只要含中文/弯引号/反引号就易解析报错：一律写成 `.py` 文件再 `python -X utf8 file.py`；`Get-Content` 读 UTF-8 无 BOM 会显示乱码，判编码/读内容用 Python，别拿 PowerShell 显示乱码当文件损坏。
- 改完必须真实浏览器逐视图（年/月/周/日）× 逐状态（明/暗、hover/active、reduced-motion）回归，并查控制台 0 报错、网络 0 个 404；canvas/iframe 类改动额外硬刷新。


### 11.6 全站字体与日历 hover 信息卡（2026-09-16）

- **字体栈（用户明确要求，勿改回）**：中文思源宋体（本地 `assets/fonts/SourceHanSerifCN-Regular.otf`，@font-face 在 styles.css 顶部，format opentype，font-display: swap）；外文/数字 Times New Roman 优先。最终栈：`--font-serif/--font-sans: "Times New Roman","Source Han Serif CN","Songti SC","STSong","Noto Serif CJK SC", serif/sans`；styles.css `:root` 与 index.html critical 内联必须保持一致。LXGW 文楷（Google Fonts 外链）已彻底删除，禁止重新引入外部字体 CDN。
- **日历 hover 信息卡**：控制器 `initCalTooltip()`（script.js，事件委托挂 document，pointerover 130ms 延迟显 / pointerout 90ms 延迟隐 / scroll capture、resize、Esc 立即隐）。卡片 `.mmv-cal-tip` 挂 **document.body**（timegrid 容器 overflow:hidden 会裁切），position:fixed 视口定位、自动翻转上下、视口 clamp。**卡片颜色只能用 `:root` 全局 token（--card/--border/--card-foreground/--muted-foreground），禁止用 --cal-* 局部变量**（那些定义在 .calendar-sheet 上，body 下取不到）。
- **数据通道**：TUI 模板根 `.mmv-tui-schedule` 带 data-sd/data-ed/data-st/data-et（allday 块带 data-allday）；月胶囊内层 title 节点的原生 title 在 hover 时移除避免双 tooltip。年视图由 `calDayIndex(y)` 按天建索引（全天事件按日期区间展开，**必须复刻 refreshCalendarSchedules 的 [title,start,end] 去重**，否则清单翻倍），日期格 `<i data-date has-schedule>` 底部圆点承载 hover；年视图卡为当天清单（色点按 12 类别色相写死，最多 12 条+溢出提示）。
- **地点兜底（用户明确要求）**：refreshCalendarSchedules 的 map 中 location 为空时填「湖北省武汉市洪山区」；7–8 月历史数据保留「江西省九江市濂溪区」。
- 本 TUI 版本月视图「N more」点击直接下钻日视图，无 floating layer；`.tui-full-calendar-month-more-schedule` 选择器保留无害。

---

> 最后更新：2026-09-16
> 本版新增：第十一节——11.6 全站思源宋体+Times New Roman 字体栈（LXGW 移除）、日历四视图 hover 信息卡（年视图 calDayIndex 索引）、日程地点缺省兜底湖北省武汉市洪山区；①Outlook 日历配色改为忠实复刻 categories（CATEGORY_TO_UI 映射 + 课程名兜底 + represented 守恒强校验 + sourceKind 审计）；②月视图降噪/日视图保详情的信息密度原则；③游戏 iframe 明暗同步三件套（父页 postMessage、子页双色板并重绘 canvas、双层版本串防缓存）；④同源三站先 diff 辨功能分叉、Restraint 克制不滥改的走查结论。
