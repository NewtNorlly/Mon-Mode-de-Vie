# 智能体操作手册

> Mon Mode de Vie 项目日常维护与内容更新标准化流程。
> 适用对象：Hanako 智能体。每次操作前通读相关章节，确保不遗漏联动步骤。

---

## 一、项目结构速查

```
D:\Mon Mode de Vie\
├── index.html              ← 主页面（导航、OTD 卡片、云菜单、音乐盒）
├── script.js               ← 核心脚本（主题、茶话、日历、聊天、初始化）
├── styles.css              ← 全局样式（含熊猫主题 CSS）
├── calendar.css             ← 日历专用样式
├── calendar-data.js         ← 日历日程数据
├── journal-data.js          ← 日记列表加载器
├── journal-module.js        ← 日记模块 UI
├── album-data.js            ← 相册数据
├── album-module.js          ← 相册模块
├── assets/                  ← 全局静态资源（头像、图标等）
├── journals/
│   ├── index.html           ← 日记归档首页
│   ├── YYYY-MM-DD.html      ← 单篇日记（四语）
│   ├── assets/
│   │   ├── journal.css
│   │   ├── journal.js
│   │   ├── journal-themes.js
│   │   └── images/          ← 日记插图
│   └── data/
│       ├── journals.json    ← 日记元数据（供 JS 加载）
│       └── journals.js      ← 日记元数据（嵌入式，window.MMV_JOURNALS）
├── .github/workflows/
│   └── deploy.yml           ← GitHub Pages 自动部署
└── 智能体操作手册.md         ← 本文档
```

---

## 二、添加日记

### 2.1 标准流程（有插图）

1. **校正原文**：修正错别字，补标点、分段落。保留口语化语气词（"嗯""诶""是的"）。
2. **生成插图**（用户明确要求时）：用 `media_generate-image`，provider 优先 MiniMax。4:3，存到 `journals/assets/images/YYYY-MM-DD-illustration.jpg`。
3. **创建日记 HTML**：参照 `journals/2026-07-30.html` 模板，四语翻译完整。关键要素：
   - `<meta name="description">` 用中文摘要
   - `<title>` 格式：`YYYY 年 M 月 D 日 · Journal · Mon Mode de Vie`
   - `<h1><time datetime="YYYY-MM-DD">` 中文日期
   - `.subtitle` 放位置
   - 四语 `<section class="prose" lang="..." data-journal-lang="...">`
   - 插图 `<figure class="journal-illustration">` 必须嵌入**全部四个语言版本**的 prose 中。每换一个语言都能看到图，不得只在中文放。
4. **更新数据文件**：
   - `journals/data/journals.json`：数组最前面插入新条目
   - `journals/data/journals.js`：同样在最前面插入（数据与 json 一致）
   - 条目格式：`date`, `file`, `location`, `excerpt`（中文 ~80 字）, `images`, `source: { label: "原创" }`, `locales: { en, fr, de }` 含 location 和 excerpt
5. **更新首页 OTD 卡片**：`index.html` 中 `#otdCard` 的 `data-journal-date`、`img src`（用 `_thumb` 缩略图版本）、`alt`、`<time>`、`.otd-excerpt` 全部换成新日记的。
6. **更新纪念日茶话**：`script.js` 中在 `var as=...` 的四个语言对象里各添加 `"YYYY-MM-DD": [...]` 条目。中文 20 条，外文对应翻译。保留历史全部条目不删除。
7. **Git 提交推送**：`git add -A; git commit -m "diary: MM/DD日记"; git push origin main`

### 2.2 无插图流程

与 2.1 相同，区别：
- 跳过生成插图步骤
- 日记 HTML 中不放 `<figure>`
- `images` 在数据文件中设为 `[]`
- OTD 卡片的 `<img>` 和 `.otd-cover` 整块可省略或保留占位

### 2.3 关键规则

- **永远不删历史茶话**：`script.js` 里的茶话是 `{"日期": [消息数组]}` 字典结构，新增条目追加即可，旧条目永久保留。
- **日期自动匹配**：茶话已实现 `new Date().toISOString().slice(0,10)` 查找当天条目，无匹配则 fallback 到最新一条。
- **数据文件双写**：`journals.json` 和 `journals.js` 必须同步更新。
- **图片必须用 WebP**：所有上线到网站的图片，无论是日记插图、相册照片还是头像图标，一律使用 WebP 格式。原图 JPG/PNG 仅保留本地源文件，HTML 和数据文件中引用的必须是 `.webp`。转换命令：`magick input.jpg -quality 85 output.webp`。

---

## 三、更新"去年今日"卡片

OTD 卡片在 `index.html` 行 118，是静态 HTML。更新步骤：

1. `data-journal-date` → 新日期
2. `img src` → 新插图路径（无插图则删除 `<img>` 或替换为占位图）
3. `alt` → 新日期
4. `<time class="otd-date">` → 如 `7月31日`
5. `.otd-excerpt` → 新摘要（与 journals.json 的 excerpt 一致 ~80 字）

**注意**：每次添加日记时 OTD 卡片必须同步更新；插图路径使用 `_thumb` 缩略图（如 `2026-07-31-illustration_thumb.jpg`），访客点击卡片进入日记后才加载原图。缩略图生成命令：`magick input.jpg -resize 400x -quality 70 output_thumb.jpg`

---

## 四、纪念日茶话管理

### 4.1 数据结构

在 `script.js` 行 21，`var as=S.lang==="en"?{日期:{...}}:...` 的四层字典中：

```js
"2026-07-31": ["消息1", "消息2", ..., "消息20"]
```

每个日期对应一个数组，中文 20 条为基准，en/fr/de 对应翻译。

### 4.2 添加新茶话

1. 在四个语言字典中各追加 `"YYYY-MM-DD": [...]` 条目（按日期排序放在最前面）
2. 中文 20 条，内容覆盖日记全部要点
3. en/fr/de 各自翻译，保持口语感和日记语气
4. 最后一个条目为该语言的收尾句（如"以上，今天的茶话"/"That's today's tea talk."）
5. **永不删除旧条目**

### 4.3 日期自动匹配机制

代码中 `var dk=new Date().toISOString().slice(0,10);var ms=as[dk]||as["最新日期"];`
- 当天有茶话 → 播当天的
- 当天无茶话 → fallback 到字典里最新那条
- 每个月至少写一条，或至少维护最近一条 fallback

---

## 五、更换音乐盒歌曲

### 5.1 文件位置

- 音频文件放在 `assets/` 目录下
- `index.html` 中 `<audio id="musicAudio" src="...">` 的 `src` 属性
- 歌曲信息在 `.music-box__body` 内的 `<p>`（歌名 + 专辑 + 年代等）

### 5.2 更换步骤

1. 将新音频文件放入 `assets/`
2. 修改 `index.html`：
   - `<source src="assets/新文件名.mp3" type="audio/mpeg">`
   - `<audio id="musicAudio" src="assets/新文件名.mp3">`
3. 更新 `.music-box__body` 中的歌曲描述文字
4. 如歌曲需要 i18n：在 `script.js` 的 `L` 对象中为 `.music-box` 相关文案添加翻译

---

## 六、修改"一封信"（作者留言）

### 6.1 数据位置

- `script.js` 行 8，`L` 对象的 `letterContent` 键（四个语言各一条）
- 中文：`L.zh.letterContent`
- 英文：`L.en.letterContent`
- 法文：`L.fr.letterContent`
- 德文：`L.de.letterContent`
- 显示在 `index.html` 中 `#authorLetter` 元素内

### 6.2 修改步骤

1. 打开 `script.js`，搜索 `letterContent`
2. 找到四个语言对应的值，逐一替换
3. 保持语义一致，语气与网站整体风格匹配（温暖、有文学感、不矫情）
4. 提交推送

---

## 七、日历日程管理

### 7.1 数据文件

`calendar-data.js`，通过 `window.MMV_CALENDAR_SCHEDULES` 暴露数据。

每一条日程格式：
```js
{
  id: "唯一ID",
  calendarId: "mmv-eat|mmv-study|mmv-teach|mmv-play|mmv-make|mmv-rest",
  title: "日程标题",
  start: "YYYY-MM-DDTHH:MM:SS",
  end: "YYYY-MM-DDTHH:MM:SS",
  location: "地点"
}
```

六类 calendarId 对应颜色：
| ID | 类别 | 颜色 |
|----|------|------|
| mmv-eat | 吃 | 橙 |
| mmv-study | 学 | 紫 |
| mmv-teach | 教 | 青 |
| mmv-play | 玩 | 粉 |
| mmv-make | 做 | 紫罗兰 |
| mmv-rest | 歇 | 灰蓝 |

### 7.2 添加快捷词条

`script.js` 行 27 的 `CAL_I18N` 对象维护了"快捷词条→各语言翻译"的映射。新增常用日程标题时同步更新四个语言的翻译。

### 7.3 添加日程

1. 编辑 `calendar-data.js`，在 `window.MMV_CALENDAR_SCHEDULES` 数组末尾追加
2. 确保 `id` 唯一（可用日期+序号）
3. `start`/`end` 用 ISO 格式
4. 如需多语言标题，在 `script.js` 的 `CAL_I18N.[lang]` 中添加对应翻译
5. 提交推送

### 7.4 修改日程

1. 在 `calendar-data.js` 中找到对应 `id`
2. 修改对应字段
3. 提交推送

### 7.5 删除日程

1. 在 `calendar-data.js` 中删除对应条目
2. 检查 `CAL_I18N` 中是否有该标题的孤立翻译（评估是否保留）
3. 提交推送

---

## 八、相册管理

### 8.1 核心约束

**永远不新增相册分册。** 现有 7 个分册是固定的，所有操作都是往已有分册中添加/删除/修改照片。

### 8.2 数据文件

- `album-data.js`：相册元数据（分册名称、封面、照片列表）
- `album-module.js`：相册 UI 逻辑
- 照片文件放在 `assets/` 或专用子目录

### 8.3 添加照片

1. 将照片文件复制到 `assets/` 目录
2. 编辑 `album-data.js`，在对应分册的 `photos` 数组中追加：
   ```js
   { file: "assets/新照片.jpg", caption: "照片说明", date: "YYYY-MM-DD" }
   ```
3. 确保 `file` 路径相对于项目根目录
4. 如有 i18n caption，在 `album-module.js` 或数据文件中补充
5. 提交推送

### 8.4 删除照片

1. 在 `album-data.js` 中删除对应条目
2. 可选：删除 `assets/` 中的源文件（评估是否被其他地方引用）
3. 提交推送

---

## 九、主题与样式

### 9.1 熊猫主题机制

- **纯 CSS 驱动**：`at()` 函数设置 `<html>` 的 `data-theme` 属性
- 导航图标切换：`.nav-icon-svg { display: none }` ↔ `.panda-emoji { display: inline }` 由 `[data-theme="panda"]` 选择器控制
- **左右栏按钮**（`#leftToggle`/`#rightToggle`）：同样挂 `.panda-emoji`（🐾），CSS 已覆盖 `.icon-button > svg`

### 9.2 修改主题色卡

在 `script.js` 的 `T` 对象中修改或添加，`TN` 存放中文名。

---

## 十、提交规范

### 10.1 Commit message 格式

- `diary: MM/DD日记 [+ 插图]` — 添加日记
- `fix: 描述` — Bug 修复
- `feat: 描述` — 新功能
- `style: 描述` — 样式调整
- `ci: 描述` — 工作流/部署
- `chore: 描述` — 维护性任务

### 10.2 推送前检查清单

- [ ] `journals.json` 和 `journals.js` 同步
- [ ] `index.html` OTD 卡片已更新
- [ ] `script.js` 茶话已追加（不删旧）
- [ ] 插图已复制到 `journals/assets/images/`
- [ ] 日记 HTML 四语完整，插图已嵌入
- [ ] `git status` 确认无意外修改
- [ ] push 后检查 GitHub Actions 部署状态

---

## 十一、快捷参考

### 常用路径

| 用途 | 路径 |
|------|------|
| 日记 HTML | `journals/YYYY-MM-DD.html` |
| 日记插图 | `journals/assets/images/YYYY-MM-DD-illustration.jpg` |
| 日记数据 | `journals/data/journals.json` + `journals.js` |
| 茶话 | `script.js` 行 21 |
| OTD 卡片 | `index.html` 行 118 |
| 一封信 | `script.js` `L.*.letterContent` |
| 音乐盒 | `index.html` `#musicAudio` `src` |
| 日历数据 | `calendar-data.js` |
| 相册数据 | `album-data.js` |
| 熊猫 CSS | `styles.css` 行 535+ |
| Workflow | `.github/workflows/deploy.yml` |

### 本地开发服务器

```cmd
启动网站.cmd    → localhost:4174
```

或手动：

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File "tools\serve-local.ps1" -Port 4180
```

---

> 最后更新：2026-08-03
> 本手册随项目演化持续更新。

---

## 附录：案例记录

### 案例 1：2026-07-31 日记插图四语补全

**问题**：新建 7/31 日记后，点击进入中文版看不到插图。
**根因**：编辑第一批次因法语段落匹配失败全部回滚，导致中文和英文的 `<figure>` 未写入文件。法文德文在第二批次单独补写成功，造成"中文缺图、其余有图"的假象。
**修复**：逐一补写 ZH 和 EN 的 `<figure class="journal-illustration">`。
**教训**：编辑含多语言模板时，永远用四个独立的小范围匹配（如段落尾句），而非一次提交四个大段替换。任何一批失败会导致整批回滚。

### 案例 2：全站有插图日记四语排查（2026-07-31）

**问题**：用户要求逐篇验证所有有插图的日记是否四语都有图。
**方法**：
```powershell
# 找到有插图的日记
Select-String -Path "journals\data\journals.json" -Pattern '"images".*\[' -Context 0,3

# 逐篇数 figure
foreach ($f in @("2026-07-31","2026-07-30","2026-07-29","2026-07-18","2025-01-19")) {
  $c = (Select-String -Path "journals\$f.html" -Pattern 'journal-illustration|journal-figure').Count
  Write-Host "$f : $c"
}
```
**发现**：
| 日期 | 插图数 | 格式 | 修复 |
|------|--------|------|------|
| 2026-07-31 | 4× journal-illustration | 新四语 | 补 ZH、EN |
| 2026-07-30 | 4× journal-illustration | 新四语 | 补 EN、FR、DE |
| 2026-07-29 | 4× journal-illustration | 新四语 | 无 |
| 2026-07-18 | 3× journal-figure | 旧 JS-i18n | 无（本身就有） |
| 2025-01-19 | 2× journal-figure | 旧 JS-i18n | 无（本身就有） |

**教训**：项目存在两种插图格式。新格式（`journal-illustration`）需四语各嵌一份；旧格式（`journal-figure` + JSON i18n）只嵌一份。排查时不能只搜一个 class。

### 案例 3：灾难级 script.js 语法错误导致全站瘫痪（2026-08-03）

**问题**：修改 `uil()` 将选项按钮翻译代码提到 `if(cal)` 外面时，编辑操作删除了条件块内部的重复翻译代码，但遗留了多余的 `}`，导致 `uil()` 函数提前关闭。script.js 加载失败，首页会话点击无响应，导航栏无响应，去年今日卡片空白。

**触发链**：
1. 原始代码：`if(cal && ...){ cal.createSchedules(...); var ga=...t("guide5"); }}var jrc=...`（`}}` 分别关闭 `if(ga)` 和 `if(cal)`，`uil()` 继续执行 `var jrc`）
2. 编辑删除 `var ga=...t("guide5");` → 变成 `cal.createSchedules(...); }}var jrc=...`
3. 第一个 `}` 关闭 `if(cal)`，第二个 `}` 提前关闭了 `uil()` → `var jrc` 变成全局代码
4. `uil()` 末尾 `});}}` 中的最后一个 `}` 变成多余的，产生 `SyntaxError: Unexpected token '}'`

**根因**：编辑 `}}` 时没有识别这两个括号分别归属谁——第一个关 `if(cal)`，第二个关的是 `if(cal)` 内嵌的 `if(ga)`，都不是关 `uil()` 的。删掉内嵌的 `if(ga)` 代码后，第二个 `}` 的位置还在，语法上就变成了关 `uil()`。

**修复**：`cal.createSchedules(sched,true);}};var jrc=` → `cal.createSchedules(sched,true);}var jrc=`，去掉多余的 `}`。

**教训**：
- **永远在编辑 JavaScript 后用 `node --check` 验证语法再推送。** 这是不可省略的步骤，尤其在编辑深层嵌套的函数体时。
- 删除括号内的代码块时，必须连同外围的 `{}` 一起删除，不能只删代码保留括号。
- 编辑 `}}` 一类的多括号时，逐对确认归属，不凭感觉。
