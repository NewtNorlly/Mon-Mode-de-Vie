# Mon Mode de Vie（MMV）设计文档

> 文档状态：实现中（与代码库同步）  
> 项目简称：MMV  
> 部署地址：`https://newtnorlly.github.io/Mon-Mode-de-Vie/`  
> 最后更新：2026-07-31

---

## 1. 项目定位

`Mon Mode de Vie` 是法语，意为"我的生活方式"。

> 让访客在一次有选择、有回应、有日期感的探索中逐渐认识作者。

一个纯粹的个人数字生活空间：日历、日记、相册、音乐盒、可切换九套主题色卡、四种语言的会客厅。不是博客，不是简历，不是社交媒体。

---

## 2. 已实现架构

### 2.1 三栏桌面布局

```
┌─────────────────────────────────────────────────────────────┐
│ [🐾左栏]    🐼 🗓️ 📝 🏔️ 🍃    导航居中    [🐾右栏]      │
├──────────┬───────────────────────────────────┬──────────────┤
│ 去年今日 │         当前主模块                  │ 留言         │
│ OTD卡片  │  Home / Now / Journal / Album     │ 音乐盒       │
│          │                                    │ 小游戏       │
└──────────┴───────────────────────────────────┴──────────────┘
```

- 左栏：「去年今日」——展示最近一篇日记的封面卡片
- 中央：四个模块（会客厅/日历/日记/相册）
- 右栏：「留言」——一封信 + 音乐盒 + 纸上漫游小游戏
- 左右栏可拖拽调整宽度，可独立开关
- 熊猫主题下左右栏开关变为 🐾 Emoji

### 2.2 五主导航

| 图标 | 模块 | 说明 |
|------|------|------|
| 🐼 | 会客厅 | 对话式引导、纪念日茶话 |
| 🗓️ | 日历 | 年/月/周/日视图，六色日程分类 |
| 📝 | 日记 | 25 篇四语日记，插图全语言嵌入 |
| 🏔️ | 相册 | 7 个固定分册 |
| 🍃 | 小云 | 主题、语言、联系方式、友情链接 |

导航常态只显示图标。熊猫主题下全部切换为 Emoji，其他主题使用 SVG。

---

## 3. 主题系统

### 3.1 九套色卡

`妮珂` `石墨` `柑橘` `湖绿` `莺紫` `钴蓝` `薰衣草` `陶土` `🐼熊猫`

每套主题包含 16 个 CSS 自定义属性（背景、前景、卡片、次要、弱化、强调、边框、侧栏），明亮/暗色各一套。

### 3.2 熊猫主题特殊行为

- 导航图标：SVG 隐藏，Emoji 显示
- 左右栏开关：显示 🐾
- 聊天气泡：更大圆角（22px），粉色调
- 切换瞬间完成，零闪烁

### 3.3 技术实现

- 纯 CSS 驱动：`at()` 函数设置 `<html>` 的 `data-theme` 和 `data-mode` 属性
- 所有主题切换通过 CSS 选择器 `[data-theme="..."]` 完成
- `localStorage` 记忆选择

---

## 4. 语言系统

- 简体中文 / English / Français / Deutsch
- 全站 i18n 存储在 `script.js` 的 `L` 对象中
- 日记为四语独立 HTML section，切语言无需 JS
- 月份、星期、界面词跟随语言
- 导航按钮有各语言的 `aria-label`

---

## 5. 会客厅（Home）

### 5.1 对话式引导

访客进入后看到逐步展开的对话。初次点击 Enter 逐步展示欢迎内容，最后呈现五个选择分支。

### 5.2 访客角色

访客头像为 `guest-little-deer.png`。点击任何选项按钮时，系统先以访客身份发送一条消息（气泡右侧、头像右侧、尖角右上、从右滑入），然后执行对应动作。

### 5.3 五个引导选项

- 最近在忙什么？ → 跳转日历
- 你平时怎么生活？ → 跳转日历
- 带我看看你珍藏的东西 → 跳转相册
- 我先随便逛逛 → 跳转日记
- 我想听听今天的纪念日茶话 → 播放当日纪念日故事

### 5.4 纪念日茶话

- 数据存储在 `script.js` 行 21，按日期索引的字典结构
- 每天 20 条消息 × 四种语言
- 自动按 `new Date()` 匹配当天茶话，无匹配则 fallback 到最新一条
- 历史茶话永久保留不删除
- 茶话播完后重新显示引导选项

---

## 6. 日历（Now）

### 6.1 底层组件

使用 TOAST UI Calendar v1.13，自定义了六色日程分类：

| 类别 | ID | 颜色 |
|------|-----|------|
| 吃 | mmv-eat | 橙 |
| 学 | mmv-study | 紫 |
| 教 | mmv-teach | 青 |
| 玩 | mmv-play | 粉 |
| 做 | mmv-make | 紫罗兰 |
| 歇 | mmv-rest | 灰蓝 |

### 6.2 年视图

- 自定义实现，不依赖 TOAST UI
- 单年展示，12 个月份迷你日历
- 标题旁 ‹ › 箭头切换年份
- 点击月份跳转月视图
- 日程数据通过 `calendar-data.js` 维护

### 6.3 月/周/日视图

- TOAST UI 原生支持
- 月视图点击日期 → 日视图
- 周视图点击日头 → 日视图
- 顶部日期跳转输入框
- 日程标题支持多语言（`CAL_I18N`）

---

## 7. 日记（Journal）

### 7.1 格式统一

全站 25 篇日记已统一为**新四语格式**：

- 四份 `<section class="prose" lang="..." data-journal-lang="...">`
- 每份内嵌 `<figure class="journal-illustration">`（如有插图）
- 切语言时通过 `hidden` 属性切换，无需 JavaScript
- 旧格式（JS-i18n + `journal-figure`）已全部淘汰

### 7.2 数据管理

- `journals/data/journals.json`：元数据（供 fetch 加载）
- `journals/data/journals.js`：嵌入式副本（`window.MMV_JOURNALS`）
- 两文件必须同步更新

### 7.3 插图规则

强制规则：有插图的日记，四个语言版本的 prose 中各嵌入一份 `figure`，切到任何语言都能看到图。

---

## 8. 相册（Album）

- 7 个固定分册，不增不减
- 分册内容：人物图鉴、人间烟火、动物图鉴、史料文献、插画壁纸、漫画图文、风光图景
- 照片数据在 `album-data.js` 维护
- 支持缩略图浏览 → 单张查看 → 前后翻页

---

## 9. 小云（Cloud）

点击顶部 🍃 展开工具箱：

- 明暗模式切换
- 九套主题色卡网格
- 四语言切换
- 联系方式（newtnorlly@outlook.com）
- 友情链接（GitHub）

交互：点击空白处或 Esc 关闭。

---

## 10. 右栏

### 10.1 一封信

标题「一封信」，点击展开/折叠。正文：

> 多做一些朝花夕拾的事，少做一些半途而废的事。

四种语言版本存储在 `L.*.letterContent`。

### 10.2 音乐盒

- 迷你黑胶唱片 UI
- 播放时唱片旋转 + 五根波形条跳动
- 可更换歌曲（见智能体操作手册）

### 10.3 纸上漫游

- 右栏内嵌 iframe 跑酷小游戏
- 基于 Chromium Dino 机制改编，使用 MMV 自有素材
- `third_party/` 保留上游源码与许可证
- 空格/上方向键/点击起跳
- 四语 + 九主题 + 明暗模式全支持

---

## 11. 聊天系统

### 11.1 消息结构

- Host 消息（NewtNorlly）：`host-mooncat.png` 头像，气泡左侧、尖角左上、从下浮入
- Guest 消息（访客）：`guest-little-deer.png` 头像，气泡右侧、尖角右上、从右滑入

### 11.2 气泡 CSS 细节

| 属性 | Host | Guest |
|------|------|-------|
| 头像 | 左列 42px | 右列 42px |
| border-radius | `4px 16px 16px 16px` | `16px 4px 16px 16px` |
| 熊猫主题 | `6px 22px 22px 22px` | `22px 6px 22px 22px` |
| 容器对齐 | 默认 | `flex + align-items: flex-end` |

---

## 12. 部署与基础设施

- **托管**：GitHub Pages（`newtnorlly.github.io/Mon-Mode-de-Vie/`）
- **CI/CD**：`.github/workflows/deploy.yml`，push 到 main 自动部署
- **Actions 版本**：checkout@v5, configure-pages@v6, upload-pages-artifact@v5, deploy-pages@v5（Node.js 24）
- **推送方式**：SSH（`git@github.com:NewtNorlly/Mon-Mode-de-Vie.git`）
- **本地服务器**：`启动网站.cmd`（端口 4174）或手动指定端口

---

## 13. 智能体操作手册

项目配备了 `智能体操作手册.md`，涵盖全部日常维护任务的标准流程：

- 添加日记（有/无插图）
- 纪念日茶话更新
- 去年今日卡片联动
- 音乐盒歌曲、一封信文案
- 日历日程管理
- 相册照片管理
- 主题与样式调整
- 全站插图排查案例

---

## 14. 已确认决策

- 网站名称 `Mon Mode de Vie`，简称 MMV
- 部署于 GitHub Pages
- 9 套主题（含熊猫），明暗双模式
- 熊猫主题为 CSS 驱动，含 Emoji 导航 + 🐾 开关
- 四语独立 HTML section，插图四语嵌入
- 全站 25 篇日记，统一新格式
- 7 个固定相册
- 聊天含访客角色（头像、气泡、动画）
- 纪念日茶话日期索引，历史永久保留
- 年视图单年 + 箭头，不再滑动百年
- 数据双写：journals.json + journals.js
- 左右栏默认展开，可拖拽

---

> 本文档随代码库同步更新。每次重大架构变更后更新对应章节和日期。
